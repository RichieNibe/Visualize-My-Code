{
    "version": 2,
    "builds": [
        {
            "src": "backend/generate_backend.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/generate_backend.py"
        }
    ]
}